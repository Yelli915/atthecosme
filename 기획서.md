# 화장품 라벨 전성분 검증 파이프라인

> **그라운딩을 적용한 멀티모달 LLM 대비 성분 재현율 +8.6%p, 건당 추론 비용 83% 절감, 추출 p95 지연 6.2초 → 1.4초**
> OCR + 하이브리드 검색 + 스키마 제약 출력으로 구성한 입점 상품 전성분 검증 보조 시스템

---

## 문서 표기 기준

| 표기 | 의미 |
| --- | --- |
| **[실측]** | 라벨링된 벤치마크셋(N=100) 대상 프로토타입 측정값 |
| **[구현]** | 프로토타입에 구현 완료된 기능 |
| **[측정 예정]** | 구현 또는 측정 계획이 있으나 아직 결과가 없는 항목 |
| **[추정]** | 가정에 기반한 값. 가정은 함께 명시 |

> `TODO` 표시는 제출 전 실제 값으로 채워야 하는 항목입니다.

---

## 1. 문제 정의

### 대상 업무

이커머스 입점 심사 시 브랜드가 제출한 **전성분 정보가 실제 패키지 라벨과 일치하는지** 확인하는 검수 업무.

### Pain Point

- **대조 작업이 길고 반복적이다.** 전성분은 제품당 수십 개 성분이 나열되며, 담당자가 라벨 사진과 제출 데이터를 한 줄씩 눈으로 대조한다.
- **누락 하나가 곧 리스크다.** 화장품은 전성분 표시 의무와 알레르기 유발 성분 표시 규정이 있어, 성분 하나의 누락·불일치도 표시 위반으로 이어질 수 있다. 이 업무에서는 "틀린 것"보다 "놓친 것"이 더 치명적이다.
- **라벨 이미지는 기계가 읽기 어렵다.** 곡면 용기, 반사광, 작고 밀집된 후면 폰트 때문에 OCR 단독으로는 신뢰할 수 있는 성분 목록을 얻기 어렵다.

### 설계 원칙

1. **완전 자동화가 아닌 선별 검수.** 사람이 모든 성분을 보는 대신, 시스템이 확신하지 못하는 항목만 사람에게 넘긴다.
2. **재현율(Recall) 우선.** 모든 설계 결정은 "성분을 놓치지 않는 것"을 1순위 기준으로 삼는다.
3. **DB에 없는 성분은 만들어낼 수 없게.** 모델이 가상의 성분명을 생성하는 경로를 구조적으로 막는다.

---

## 2. 아키텍처 비교 검증 [실측]

동일한 벤치마크셋 100건(단상자·용기 사진: 정면, 곡면, 조명 반사 포함)으로 4개 구조를 비교했습니다.

| 구분 | 안 1: 멀티모달 LLM 단독 | 안 2: 멀티모달 LLM + RAG | 안 3: OCR + 정규식 | **안 4 (채택): OCR + RAG + 구조화 출력** |
| --- | --- | --- | --- | --- |
| 구성 | Image → GPT-4o-mini | Image → GPT-4o-mini → DB 검색 → 재검증 | Image → `TODO: OCR 엔진명` → Regex | Image → `TODO: OCR 엔진명` → 하이브리드 검색 → `TODO: 모델명` (Structured Output) |
| 제품명 정확도 (EM) | 81.0% | 91.0% | 53.0% | **94.0%** |
| 성분 재현율 | 78.4% | 88.2% | 46.5% | **96.8%** |
| 추출 지연 (p95) | 4.6초 | 6.2초 | 0.9초 | **1.4초** |
| 건당 비용 | $0.012 | $0.018 | $0.001 | **$0.003** |

- 측정 시점: `TODO: YYYY-MM`
- 벤치마크셋 라벨링: `TODO: 라벨링 방식 (예: 제품 공식 전성분 표기와 대조하여 수기 작성)`

### 채택 근거: 안 2 vs 안 4

가장 강한 경쟁안인 안 2(그라운딩을 붙인 멀티모달)와 비교해도 안 4가 세 지표에서 모두 앞섰습니다.

| 지표 | 안 2 | 안 4 | 차이 |
| --- | --- | --- | --- |
| 성분 재현율 | 88.2% | 96.8% | **+8.6%p** |
| 건당 비용 | $0.018 | $0.003 | **약 83% 절감** |
| 추출 지연 (p95) | 6.2초 | 1.4초 | **약 77% 단축** |

**해석:** 후면 라벨처럼 작은 글자가 밀집된 이미지에서는 멀티모달 모델이 성분을 건너뛰는 경향이 관찰되었습니다. 전용 OCR로 텍스트를 먼저 빠짐없이 뽑고, 이후 단계를 텍스트 처리로만 구성한 것이 재현율과 비용 모두에 유리했습니다. `TODO: 멀티모달 모델이 성분을 누락한 대표 사례 1~2건 요약`

---

## 3. 파이프라인 설계 [구현]

```
[입력] 라벨 이미지 + 브랜드 제출 전성분 데이터
   │
   ▼
[Stage 1] OCR ─ 텍스트 + Bounding Box 좌표 추출
   │
   ▼
[Stage 2] 하이브리드 검색 ─ 성분 마스터 DB 대상 BM25 + Dense Vector, OCR 토큰별 Top-5 후보
   │
   ▼
[Stage 3] 스키마 제약 정규화 ─ Structured Output, 필드 Enum = [후보 ID 목록 + "NOT_FOUND"]
   │
   ▼
[Stage 4] 대조 ─ 라벨에서 추출한 성분 ID 집합 vs 제출 데이터 성분 ID 집합
   │          → 누락 / 추가 / 불일치 목록 생성
   ▼
[Stage 5] 신뢰도 분기
   ├─ 전 항목 고신뢰 + 불일치 없음 ──▶ 원클릭 승인
   └─ 저신뢰 / NOT_FOUND / 불일치 존재 ──▶ 집중 검수 (해당 영역 이미지 크롭 제공)
```

> Stage 4(대조)의 구현 상태: `TODO: [구현] 또는 [측정 예정]으로 표기`

### 환각 방지: 스키마 수준 제약

- 자유 텍스트 생성이 아닌 Strict JSON Schema 기반 구조화 출력을 사용합니다.
- 각 성분 필드의 허용값을 **요청마다 동적으로 생성한 Enum**(검색된 후보 ID + `NOT_FOUND`)으로 제한합니다.
- 따라서 DB에 없는 성분명은 출력 문법상 생성될 수 없습니다.

### 이 구조가 막지 못하는 오류

제약 출력은 "없는 성분 생성"을 막을 뿐, 아래 오류는 여전히 발생할 수 있습니다. 이 오류들은 신뢰도 분기와 사람의 검수로 관리합니다.

- **후보 중 잘못된 선택:** 정답이 후보에 있지만 유사한 다른 성분을 고르는 경우
- **검색 실패:** 정답이 Top-5 후보에 없는 경우
- **OCR 누락:** 라벨의 텍스트를 OCR이 아예 읽지 못한 경우. 이후 어떤 단계로도 복구할 수 없으므로, **파이프라인 전체 재현율의 상한은 OCR이 결정합니다.**

---

## 4. 신뢰도 분기 및 임계치 선정

### 신뢰도 점수

검색 유사도 점수와 구조화 출력의 토큰 로그 확률을 결합해 성분별 신뢰도를 산출합니다. `TODO: 결합 방식 (예: 가중 평균, 가중치 값)`

### 임계치 선정 방식 [측정 예정]

임계치는 임의로 정하지 않고, 벤치마크셋에서 임계치별로 아래 두 값을 측정해 결정합니다.

- **자동 승인 비율:** 전체 건 중 원클릭 승인으로 분류되는 비율 (높을수록 공수 절감)
- **자동 승인 건 내 누락률:** 원클릭 승인된 건 중 실제로 성분이 누락·오매핑된 비율 (0에 가까워야 함)

| 임계치 | 자동 승인 비율 | 자동 승인 건 내 누락률 |
| --- | --- | --- |
| 0.85 | `TODO` | `TODO` |
| 0.90 | `TODO` | `TODO` |
| 0.95 | `TODO` | `TODO` |

선정 기준: **자동 승인 건 내 누락률이 목표치 이하인 범위에서 자동 승인 비율이 가장 높은 값.** 이 과정에서 신뢰도 점수가 실제 정답 확률과 맞는지(보정 상태)도 함께 확인합니다.

---

## 5. 평가 체계

### 5.1 데이터셋

- **벤치마크셋 [실측]:** 100건 라벨링 완료, 본 문서의 실측치 산출에 사용
- **확장셋 [측정 예정]:** 150건으로 확장 중 (곡면 50, 반사광 50, 소형 라벨 50)

> `TODO: 100건과 150건의 관계를 사실대로 기재 (예: 100건 포함 여부)`

### 5.2 추출 지표

| 지표 | 정의 | 용도 |
| --- | --- | --- |
| 제품명 EM | 정규화된 제품명의 완전 일치 비율 | 제품 식별 정확도 |
| 성분 재현율 | 정답 성분 중 추출된 비율 | **핵심 지표** (누락 방지) |
| 성분 정밀도 | 추출 성분 중 정답인 비율 | 검수자 부담 관리 |

### 5.3 불일치 탐지 평가 [측정 예정]

실제 불일치 사례를 모으기 어려우므로, 정답 성분 목록을 인위적으로 변형한 "제출 데이터"를 만들어 탐지율을 측정합니다.

- **변형 유형:** 성분 누락, 성분 추가, 유사 성분으로 치환
- **지표:** 유형별 불일치 탐지율, 정상 건의 오탐률

---

## 6. 오류 분석 [실측 기반 작성 필요]

안 4에서 놓친 성분(재현율 96.8% 기준 약 3.2%)을 원인별로 분류합니다. 이 분류가 Sprint 2 이전 개선 우선순위를 결정합니다.

| 오류 원인 | 건수 | 비율 | 대표 사례 | 개선 방향 |
| --- | --- | --- | --- | --- |
| OCR 단계 누락 (텍스트 미인식) | `TODO` | `TODO` | `TODO` | 촬영 가이드, 다중 촬영, OCR 엔진 비교 |
| 검색 실패 (정답이 후보에 없음) | `TODO` | `TODO` | `TODO` | 동의어·이표기 사전 보강, Top-K 조정 |
| 후보 중 오선택 | `TODO` | `TODO` | `TODO` | 후보 메타데이터 보강, 임계치 조정 |

---

## 7. 성과 지표

| 지표 | 기준선 | 현재 | 목표 | 비고 |
| --- | --- | --- | --- | --- |
| 성분 재현율 | 88.2% (안 2) | **96.8%** [실측] | 99.0% 이상 | 핵심 지표 |
| 제품명 EM | 91.0% (안 2) | **94.0%** [실측] | 95.0% 이상 | |
| 추출 지연 (p95) | 6.2초 (안 2) | **1.4초** [실측] | 1.5초 이내 유지 | 이미지 입력 → 정규화 완료 |
| 건당 비용 | $0.018 (안 2) | **$0.003** [실측] | $0.003 이하 유지 | OCR + 모델 호출 합산 |
| 자동 승인 비율 | - | `TODO` [측정 예정] | `TODO` | 4장 임계치 분석 결과 |
| 불일치 탐지율 | - | `TODO` [측정 예정] | `TODO` | 5.3 변형 데이터 기준 |
| 건당 검수 시간 | `TODO` [측정 예정] | `TODO` [측정 예정] | - | 샘플 10건, 수기 대조 vs 파이프라인 보조 비교 |

---

## 8. 한계

- **표본 규모:** N=100은 방향을 판단하기에는 충분하지만, 99% 수준의 재현율을 통계적으로 검증하기에는 작습니다. 확장셋으로 재측정이 필요합니다.
- **DB 커버리지:** 성분 마스터 DB에 없는 신규 원료는 항상 `NOT_FOUND`가 되므로, 이 비율을 별도로 추적해야 합니다.
- **촬영 품질 의존:** OCR이 파이프라인 재현율의 상한을 결정하므로, 입력 이미지 품질 관리가 모델 개선만큼 중요합니다.

---

## 9. Sprint 2 (후속 과제)

Sprint 1은 "라벨에서 성분을 빠짐없이 정확히 추출하고 대조하는 것"에 집중했습니다. 아래 항목은 성격이 다르거나 Sprint 1의 결과에 의존하므로 후속 과제로 분리했습니다.

- **규제 성분 하이라이트:** 금지 원료, 알레르기 유발 성분을 검수 화면에서 강조. Stage 3의 정규화된 성분 ID를 그대로 활용 가능.
- **B2C 리뷰 작성 보조:** 이커머스에서는 구매 이력으로 제품을 이미 알 수 있어 OCR의 가치가 낮습니다. 따라서 **다른 곳에서 구매한 제품을 리뷰하는 플랫폼** 시나리오로 한정해 검증합니다. 리뷰 문장 생성, 톤 프리셋, 문장 단위 편집 UI를 포함합니다.
- **생성 품질 평가:** 리뷰 텍스트 평가를 위한 LLM-as-a-Judge 도입. 사람 평가자 간 일치도를 먼저 확인한 뒤 Judge와의 일치도(가중 카파)를 검증합니다.
- **수정률 기반 피드백 루프:** 사용자 수정률로 프롬프트 품질을 모니터링. 무수정 채택 문장은 샘플링 검토 후에만 예시로 편입합니다.