# Sprint 1 마무리 실행 계획

> 목표: 기획서의 모든 `TODO`를 **실제 측정값**으로 채워 제출 가능한 상태로 만든다.
> 원칙: 측정하지 않은 값은 채우지 않는다. 못 채운 항목은 [측정 예정]으로 남긴다.

---

## 순서 요약

| 단계 | 작업 | 채워지는 기획서 항목 | 난이도 |
| --- | --- | --- | --- |
| 1 | 사실 정리 | 2장 모델명·시점·라벨링 방식, 5.1 데이터셋 관계 | 낮음 |
| 2 | 오류 분석 | 2장 누락 사례, 6장 오류 분석 표 | 중간 |
| 3 | 대조(Stage 4) 구현 및 평가 | 3장 구현 상태, 5.3, 7장 불일치 탐지율 | 중간 |
| 4 | 신뢰도 결합 및 임계치 분석 | 4장 결합 방식·임계치 표, 7장 자동 승인 비율 | 중간 |
| 5 | 검수 시간 측정 | 7장 건당 검수 시간 | 낮음 |
| 6 | 문서 반영 및 면접 준비 | 전체 | 낮음 |

---

## 1. 사실 정리

기억이 아니라 코드·로그·설정 파일에서 확인해 기록한다.

- [ ] 실제 사용한 OCR 엔진명, 구조화 출력 모델명
- [ ] 벤치마크 측정 시점 (YYYY-MM)
- [ ] 정답 라벨 작성 방식 (무엇과 대조해 만들었는지)
- [ ] 성분 마스터 DB의 출처와 규모 (직접 확인한 값)
- [ ] 100건 벤치마크셋과 150건 확장셋의 관계
- [ ] 벤치마크셋 전체 정답 성분 개수 (재현율 분모)

## 2. 오류 분석

- [ ] 안 4에서 놓친 성분 전체 목록 추출
  - 참고: 이미지 100건 × 제품당 약 30개 성분이면 분모 약 3,000개, 재현율 96.8% 기준 누락은 약 90개 안팎. 실제 분모로 다시 계산할 것
- [ ] 누락 성분마다 원인 분류: OCR 누락 / 검색 실패(정답이 Top-5에 없음) / 후보 중 오선택
  - 판별법: OCR 텍스트에 해당 성분이 있는가 → 검색 후보에 정답이 있는가 → 모델이 고른 값은 무엇인가
- [ ] 원인별 대표 사례 1건씩 이미지와 함께 보관 (면접 시 제시용)
- [ ] 안 1·2(멀티모달)에서 성분이 누락된 대표 사례 1~2건 확보
- [ ] 검색 실패 중 오탈자가 원인인 건수 별도 집계 (BM25 한계 및 n-gram 검색 도입 근거)

## 3. 대조(Stage 4) 구현 및 평가

- [ ] 추출 성분 ID 집합과 제출 데이터 성분 ID 집합 비교 로직 구현 (누락 / 추가 / 불일치)
- [ ] 정답 목록을 변형한 가짜 제출 데이터 생성: 성분 누락, 성분 추가, 유사 성분 치환
- [ ] 변형 없는 정상 제출 데이터도 함께 준비 (오탐률 측정용)
- [ ] 측정: 유형별 불일치 탐지율, 정상 건 오탐률
- 시간이 부족하면 구현하지 말고 기획서에 [측정 예정]으로 유지

## 4. 신뢰도 결합 및 임계치 분석

- [ ] 결합 방식 후보 2~3개 비교 (예: 가중 평균, 최소 토큰 확률 사용 여부)
- [ ] 임계치 0.85 / 0.90 / 0.95 별 자동 승인 비율과 자동 승인 건 내 누락률 측정
- [ ] 신뢰도 구간별 실제 정답률 확인 (점수가 높을수록 실제로 정확한지)
- [ ] 선정 기준에 따라 결합 방식과 임계치 확정, 선택 이유 한 줄 기록

## 5. 검수 시간 측정

- [ ] 샘플 10건 선정
- [ ] 수기 대조 시간 측정 (라벨 사진과 제출 데이터를 눈으로 비교)
- [ ] 파이프라인 보조 시 검수 시간 측정 (집중 검수 항목만 확인)
- [ ] 가능하면 본인 외 1~2명에게 부탁해 편차 확인
- 표본이 작다는 점을 기획서에 그대로 명시

## 6. 문서 반영 및 면접 준비

- [ ] 측정 완료 항목의 표기를 [측정 예정] → [실측] / [구현]으로 변경
- [ ] 헤드라인 수치가 최종 측정값과 일치하는지 재확인
- [ ] 예상 질문 답변을 **실제 구현 기준으로** 작성
  - OCR 오탈자 시 검색이 실패하지 않는가 → 2단계 오탈자 집계로 답변
  - 성분 DB는 어디서 구축했는가 → 1단계 확인 내용으로 답변
  - 96.8%면 실무에 쓸 수 있는가 → 4·5단계 측정값으로 답변
- [ ] 오류 사례 이미지 3~4장을 바로 꺼낼 수 있게 정리

---

## 완료 기준

- 기획서에 `TODO`가 남아 있지 않다 (측정 못 한 항목은 [측정 예정]으로 명시)
- 모든 [실측] 수치에 대해 원본 데이터나 로그를 제시할 수 있다
- 오류 원인별 실제 사례를 하나씩 보여줄 수 있다