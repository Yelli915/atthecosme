# 7. 파이프라인 코드 구현 (Stage 1~5)

- 상태: 진행중 (코드 작성 완료, 실제 이미지·벤치마크셋으로 미검증)
- 일시: 2026-09-24
- 계획.md 범위 밖 작업: 계획.md는 "코드가 이미 있다"는 전제로 사실 정리부터 시작했지만,
  실제로는 코드가 없었기 때문에(00-setup.md 참고) 이 단계에서 파이프라인 자체를 처음부터 구현했다.
  계획.md 1~6단계는 이 구현 이후에 진행한다.

## 스택 결정

| Stage | 선택 | 비고 |
| --- | --- | --- |
| 1. OCR | PaddleOCR (로컬) | `pipeline/ocr/engine.py` |
| 2. 하이브리드 검색 | SQLite + rank_bm25 + FAISS(sentence-transformers 임베딩) | `pipeline/retrieval/` |
| 3. 구조화 출력 | Ollama 로컬 LLM (`qwen2.5:7b`), JSON Schema `format` 파라미터로 Enum 제약 | `pipeline/structured_output/normalize.py` |
| 4. 대조 | 규칙 기반 ID 집합 비교 (누락/추가/불일치) | `pipeline/match/match.py` |
| 5. 신뢰도 분기 | Stage 2 검색 신뢰도 + Stage 3 토큰 신뢰도(logprobs)의 최솟값 결합 (임시), 임계치 0.90 플레이스홀더 | `pipeline/run.py`, `pipeline/config.py` |

## 만든 파일

```
requirements.txt
pipeline/config.py                     # 경로·모델명·임계치 전역 설정
pipeline/ocr/engine.py                 # Stage 1
pipeline/retrieval/db.py               # 성분 마스터 DB (SQLite)
pipeline/retrieval/bm25_index.py       # Stage 2 (BM25)
pipeline/retrieval/vector_index.py     # Stage 2 (FAISS)
pipeline/retrieval/hybrid.py           # Stage 2 (결합)
pipeline/structured_output/normalize.py # Stage 3
pipeline/match/match.py                # Stage 4
pipeline/run.py                        # Stage 1~5 오케스트레이션
eval/metrics.py                        # 재현율/정밀도/EM
eval/run_benchmark.py                  # 벤치마크 실행 스크립트
data/ingredient_db/seed_ingredients.csv # 샘플 15건 (실제 DB 아님, 동작 확인용)
data/benchmark/manifest.example.json    # 벤치마크 manifest 포맷 예시
.gitignore
```

## 확인한 것 (이번 세션에서 실제로 실행/검증)

- `python -m pipeline.retrieval.db` → SQLite DB 생성 및 15건 적재 확인 (`pipeline/retrieval/db.py`, stdlib만 사용해 의존성 설치 없이 검증 가능)
- 전체 `.py` 파일 `py_compile` 문법 검사 통과

### 2026-09-25 추가 확인

- 의존성 설치: `.venv`에서 `pip install -r requirements.txt` 완료 확인 (paddlepaddle 3.3.1, ollama 0.6.2 등). 단, 전역/사용자 Python으로 설치 시 `paddlepaddle`이 인덱스에서 아예 안 잡히는 문제가 있었음 — 반드시 프로젝트 `.venv` 활성화 후 설치해야 함.
- 로컬 LLM: `qwen2.5:7b` 대신 `ollama pull hf.co/Qwen/Qwen2.5-7B-Instruct-GGUF`로 받음 → 로컬 태그명이 `hf.co/Qwen/Qwen2.5-7B-Instruct-GGUF:latest`(3.0GB)로 달라져 `pipeline/config.py`의 `OLLAMA_MODEL`을 이 태그로 갱신함. README의 `qwen2.5:7b` 안내와 실제 사용 태그가 달라졌으므로 문서 갱신 필요.
- Stage 2+3 실동작 검증 (`ollama.chat` 실제 호출, 샘플 DB 15건 기준):
  - `나이아신아마이드` → 검색 1위 `Niacinamide` 정상 선택 (`chosen_ingredient_id=5`)
  - `히알루론산` → 이표기(alias) 경유로 `Sodium Hyaluronate` 정상 선택 (`chosen_ingredient_id=7`)
  - `존재하지않는가짜성분XYZ` → DB에 없는 성분을 후보 중 하나로 잘못 고르지 않고 `NOT_FOUND`로 정확히 거부 — 환각 방지(동적 Enum 제약) 의도대로 동작 확인
  - 근거: 임시 스크립트로 `HybridRetriever.search` + `normalize_token`을 이어서 실행, 원본 모델 응답(`raw_model_output`)까지 확인 (`{"ingredient_id": "cand_5"}` 등)

### 2026-09-25 추가: Stage 5 로그 확률 결합 구현 (사진 없이 가능한 작업)

- `ollama.chat(..., logprobs=True, top_logprobs=N)`이 실제로 토큰별 `logprob`을 반환하는지 직접
  호출로 확인 — 지원됨. (`pipeline/run.py`에 있던 TODO 해소)
- 최초 구현은 응답 전체 토큰 중 최솟값을 썼더니, `{"ingredient_id": ...}`의 **키 이름
  토큰(`ingredient`, `_id` 등)의 확률이 낮게 나와** 모델의 실제 선택과 무관한 노이즈가
  신뢰도를 지배하는 문제를 발견 (예: 확신 있는 정상 매칭인데도 token_confidence≈0).
  → `_value_token_confidence()`를 추가해 **선택값(`cand_5`, `NOT_FOUND` 등) 문자열에 해당하는
  토큰만** 골라 최솟값을 쓰도록 수정.
- 수정 후 재검증: 확신 있는 정상 매칭(`나이아신아마이드`→`cand_5`)은 token_confidence≈1.0,
  애매한 가짜 성분의 NOT_FOUND 판정은 0.74로 상대적으로 낮게 나옴 — 의도한 신호로 동작 확인.
- `pipeline/run.py`의 `TokenNormalization.combined_confidence = min(검색 신뢰도, 토큰 신뢰도)`로
  Stage 5에 반영. **결합 방식(최솟값 vs 가중평균 등) 비교·최종 확정은 실측 필요 — 아래 미확인 항목 참고.**

### 2026-09-25 추가: 라벨 이미지 없이 가능한 단위 테스트 정식화

`eval/metrics.py`(원래부터 "이미지 없이 단위 테스트 가능"하게 설계됨), 신규 추가한
`_value_token_confidence`(위 로그 확률 버그의 회귀 방지), `pipeline/match/match.py`
(대조 시나리오)에 대해 `test_*.py` 3개 파일로 테스트를 고정함:

- `eval/test_metrics.py` (13개) — 재현율/정밀도/EM/마이크로평균
- `pipeline/structured_output/test_normalize.py` (4개) — 위에서 발견한 "키 이름 토큰이
  신뢰도를 왜곡" 버그의 재발 방지
- `pipeline/match/test_match.py` (6개) — records/03의 시나리오를 재실행 가능하게 이전.
  `SUBMITTED_FUZZY_ACCEPT_THRESHOLD` 위험 사례도 "현재 동작 문서화"로 포함 (임계치
  실측 후 이 테스트도 같이 갱신 필요)

`python -m unittest discover -p "test_*.py"`로 전체 23개 테스트 통과 확인. README
"실행 방법"에 반영.

## 아직 확인 못한 것 (실 데이터 없음)

- Stage 1 (PaddleOCR) 실제 동작 — 실제 라벨 이미지가 아직 없어 미검증
- Stage 2 Dense 검색(FAISS) 자체는 위 스모크 테스트에서 간접 확인됐으나, 실제 라벨 OCR 결과(오탈자·부분 인식 포함)를 넣은 검증은 아님
- 실제 라벨 이미지, 실제 성분 마스터 DB(현재는 샘플 15건), 실제 벤치마크 manifest 전부 없음 → `eval/run_benchmark.py` 실행 불가, `run_pipeline()` 전체(Stage 1~5) 통합 실행 미검증
- Stage 5 신뢰도 결합 방식(최솟값 vs 가중평균 등 후보 비교, 임계치 0.85/0.90/0.95별 자동승인 비율·누락률)의 실측 확정 — `records/04-신뢰도임계치.md`, 계획.md 4단계 대상. 지금은 최솟값 결합을 임시로 쓰는 상태
- Stage 4의 `SUBMITTED_FUZZY_ACCEPT_THRESHOLD=0.7`도 같은 실측 대상에 포함 필요 (`records/03-대조구현.md`에서 발견한 오매핑 위험)

## 다음 단계

1. ~~README의 Ollama 모델명 안내(`ollama pull qwen2.5:7b`)를 실제 사용 태그와 일치시키기~~ 완료
2. 성분 마스터 DB 실제 출처 확보 (계획.md 1단계, `records/01-사실정리.md`)
3. 실제 라벨 이미지 확보 후 `run_pipeline()`으로 Stage 1~5 통합 실행 → 코드 결함 먼저 잡기 (사용자 결정: 추후 진행)
4. 이후 `계획.md` 1단계(사실 정리)부터 순서대로 진행 — 4단계(신뢰도 임계치)는 실 벤치마크 없이는 결합 방식 비교·임계치 확정이 불가능
